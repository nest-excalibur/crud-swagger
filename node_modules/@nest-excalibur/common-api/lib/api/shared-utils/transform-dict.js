"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformDict = void 0;
function transformDict(dict, key) {
    const keys = Object.keys(dict);
    return keys.reduce((accumulator, objectKey) => {
        const value = dict[objectKey];
        if (value) {
            const subValue = value[key];
            if (subValue) {
                accumulator[objectKey] = subValue;
            }
        }
        return accumulator;
    }, {});
}
exports.transformDict = transformDict;
