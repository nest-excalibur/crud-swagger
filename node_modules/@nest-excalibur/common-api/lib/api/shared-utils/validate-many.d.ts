import { ClassType } from 'class-transformer/ClassTransformer';
import { ValidationError } from 'class-validator';
import { ValidationResponse } from '../interfaces/validation-interface';
export declare function validateMany<T extends {
    [k: string]: any;
}>(records: T[], dtoClass: ClassType<T>): Promise<ValidationError[]>;
export declare function parseAndValidateMany<T extends {
    [k: string]: any;
}>(records: T[], dtoClass: ClassType<T>): Promise<{
    parsedData: T[];
    errors: ValidationResponse<T>[];
}>;
