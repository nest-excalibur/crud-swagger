import { ArgumentMetadata, PipeTransform } from '@nestjs/common';
import { BaseDTO } from '../schemas/base-dto';
export declare class DefaultValidationPipe implements PipeTransform {
    protected dto: typeof BaseDTO | (new () => any);
    protected showErrors: boolean;
    protected validateId?: boolean | undefined;
    constructor(dto: typeof BaseDTO | (new () => any), showErrors?: boolean, validateId?: boolean | undefined);
    transform(value: any, metadata: ArgumentMetadata): Promise<any>;
    protected toValidate(metatype: Function): boolean;
}
