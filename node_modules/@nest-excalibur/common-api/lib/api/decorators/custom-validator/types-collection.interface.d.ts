import { ValidationArguments } from 'class-validator';
export interface HasMap {
    [key: string]: any;
}
export declare type ValidationFunction = (value: any, ...args: any[]) => boolean;
export interface TypesCollectionInterface {
    [k: string]: ValidationFunction;
}
export declare type TypesValidator = Record<string, ValidationFunction>;
export declare type TypesCollectionArguments<T = TypesCollectionInterface> = {
    [k in keyof T]: HasMap | ValidationArguments;
};
