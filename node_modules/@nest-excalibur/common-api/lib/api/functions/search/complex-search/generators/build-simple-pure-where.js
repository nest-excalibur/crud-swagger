"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildSimplePureWhere = void 0;
const verificator_helper_1 = require("../verificators-functions/verificator-helper");
function buildSimplePureWhere(attribute, value, entityName, index = 1) {
    const strParameterKey = `valorAtributo${index}${entityName}${attribute}`;
    const initialParemeters = {};
    if (!verificator_helper_1.VerificatorHelper.isAttKeyWord(attribute)) {
        initialParemeters[strParameterKey] = value;
        return {
            where: `${entityName}.${attribute}=:${strParameterKey}`,
            parameters: initialParemeters,
            conjunction: 'and',
        };
    }
    else {
        return undefined;
    }
}
exports.buildSimplePureWhere = buildSimplePureWhere;
