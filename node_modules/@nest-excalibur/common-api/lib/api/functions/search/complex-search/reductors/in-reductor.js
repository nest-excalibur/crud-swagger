"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.inReductor = void 0;
function inReductor(values) {
    const length = values.length;
    return values.reduce((acumulator, currentValue, index) => {
        const isNumber = typeof currentValue === 'number';
        const isString = typeof currentValue === 'string';
        const isLast = index + 1 === length;
        if (isNumber) {
            acumulator += currentValue;
        }
        if (isString) {
            acumulator += `"${currentValue}"`;
        }
        acumulator += `${isLast ? '' : ','}`;
        return acumulator;
    }, '');
}
exports.inReductor = inReductor;
